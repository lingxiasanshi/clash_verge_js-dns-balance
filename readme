功能说明
用ai集合了这两位的脚本
https://github.com/xiaolin-007/clash-verge-script
https://github.com/KaranocaVe/ClashVergeProfilePatchScript

#三种智能代理模式：
**均衡负载** - 散列：基于请求特征分配到固定节点，适合需要会话保持的场景（如登录状态）
**均衡负载** - 轮询：按顺序循环使用节点，最大化利用所有节点带宽
**自动选择**：定期测试节点延迟，自动选择最优节点（延迟差在 50ms 内视为等效）
#无缝集成：
新代理组会自动添加到所有现有代理组的可选列表顶部
继承了原有代理组的基础配置（如检测间隔、超时时间）
仅在存在有效节点时才创建这些组，避免配置错误
#使用场景建议：
多节点用户可通过负载均衡提升并发访问速度
自动选择适合希望 "一键最优" 的用户
不同服务（如流媒体 / AI）可分别选择最适合的负载模式
如果需要调整负载均衡的检测 URL 或策略参数，可以修改对应代理组的url、interval或strategy字段。
